mutation SignUp(
    $email: String!
    $password: String!
    $firstName: String!
    $lastName: String!
    $receivePromo: Boolean!
) {
    signUp(
        email: $email
        password: $password
        firstName: $firstName
        lastName: $lastName
        receivePromo: $receivePromo
    )
}
